# Offenes Notizbuch — nikolay-baut.de (Arbeitstitel)

Statische Astro-Site. Inhalte als Markdown in `src/content/{feldnotizen,gebaut}/`.

## Entwickeln
    npm install
    npm run dev      # http://localhost:4321
    npm run build    # statischer Export nach dist/

## Veröffentlichen
Schreiben = Markdown-Datei committen. Frontmatter-Felder siehe `src/content/config.ts`.

Offene Punkte: siehe CLAUDE.md (TODO-Liste).
